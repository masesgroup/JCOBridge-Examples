remote_theme: "mmistakes/minimal-mistakes@4.20.1"

plugins:
  - jekyll-include-cache
  - jekyll-sitemap

defaults:
  # _docs
  - scope:
      path: ""
      type: docs
    values:
      layout: single
      share: true
      sidebar:
        nav: "docs"
